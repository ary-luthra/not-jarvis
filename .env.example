SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token
OPENAI_API_KEY=sk-your-openai-key

# OPENAI_MODEL=gpt-5.2
# SYSTEM_PROMPT=You are a helpful assistant.
# MEMORY_DIR=memory

# --- Opik (LLM observability) ---
# Tracing is only active when BOTH the --trace CLI flag is passed AND OPIK_API_KEY is set.
# Usage: python bot.py --trace
# OPIK_API_KEY=your-opik-api-key
# OPIK_WORKSPACE=your-workspace-name   # defaults to "default"
# OPIK_PROJECT_NAME=not-jarvis         # groups traces in the Opik dashboard
